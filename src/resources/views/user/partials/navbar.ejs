<div class="navbar">
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link">
                <i class="fas fa-bars" onclick="collapseSidebar()"></i>
            </a>
        </li>

        <li class="nav-item">
            <img src="/img/Logodental.png" alt="" class="logo">
            <img src="/img/chulogo.png" alt="" class="title-logo">
        </li>
    </ul>

    <div class="navbar-nav nav-right">
        <% if (_session.roleName == 'admin') { %>
            <a href="/administration">
                <p class="navbar__name">Admin <%= _session.user.fullname %></p>
                <div class="navbar__img">
                    <img src="<%= _session.user.image %>" alt="">
                </div>
            </a>
        <%} else { %>
            <a href="/myaccount">
                <% if (_session.roleName == 'Nha sĩ') { %>
                    <p class="navbar__name">BS <%= _session.user.fullname %></p>
                <%} else { %>
                    <p class="navbar__name">LT <%= _session.user.fullname %></p>
                <% } %>   
                <div class="navbar__img">
                    <img src="<%= _session.user.image %>" alt="">
                </div>
            </a>
        <% } %>
    </div>
</div>